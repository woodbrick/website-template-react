import Pagination from './pagination'
import React from 'react'

class PagedListTable extends React.component {
  render() {
    return (
      <Pagination />
    )
  }
}

export default PagedListTable
